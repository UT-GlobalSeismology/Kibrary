package io.github.kensuke1984.kibrary.firsthandler;

import java.nio.file.Path;

public class SacTriplet {

    SacTriplet(Path sacPath){
        //set variables
        //register
    }

/*
    boolean add(Path sacPath) {
        //if variables are same, register
//        return rootSacFileName.isRelated(sacFileName) && nameSet.add(sacFileName);
    }
*/
    private void register(Path sacPath) {
        //register R or T or Z
    }

}
